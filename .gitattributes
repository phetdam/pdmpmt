# use LF line endings for all other text files
* text=auto
# batch files should always be CRLF, can run into some weird errors with
# labels if they are stored using LF as the line ending
*.bat text eol=crlf
